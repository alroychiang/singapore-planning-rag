# --- Version control ---
.git/
.gitignore

# --- Environments ---
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd

# --- Secrets ---
.env

# --- Data that doesn't ship in the image ---
# Raw PDFs — the pipeline was run beforehand; only the Chroma index ships
data/raw/
# Intermediate artifacts — same reason
data/processed/chunks.jsonl
data/processed/embeddings.npy
# Eval artifacts — for dev, not runtime
data/eval/
reports/

# --- OS / IDE junk ---
.DS_Store
.vscode/
.idea/
*.swp

# --- Docker itself ---
Dockerfile
.dockerignore

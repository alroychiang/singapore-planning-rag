# --- Core RAG pipeline ---
chromadb>=0.5.0
sentence-transformers>=2.7.0
numpy>=1.24.0

# --- PDF extraction (only used at build time, but small enough to keep) ---
pdfplumber>=0.10.0
pypdf>=4.0.0

# --- LLM backends ---
google-genai>=0.3.0
ollama>=0.3.0

# --- Web UI ---
streamlit>=1.30.0

# --- Config / secrets ---
python-dotenv>=1.0.0
